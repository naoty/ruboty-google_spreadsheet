require 'spec_helper'

describe Ruboty::GoogleSpreadsheet do
  it 'has a version number' do
    expect(Ruboty::GoogleSpreadsheet::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
